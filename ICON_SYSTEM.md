# Quick Links UI Icon System

> 2026-08-13

## Why

今回の発端は、丸いFABの中央に置いた文字の「＋」が、CSS上は中央でも視覚的にはずれて見えたこと。

原因は、UI記号をフォントの字形へ依存させると、baseline、line-height、フォント固有の字面、OS差が操作部品の見た目へ混ざるため。

Quick Linksでは、機能アイコンを「文字」ではなく「UI形状」として扱う。

## Principles

1. **機能アイコンを文字・絵文字へ依存させない**
   - `＋` `×` `…` や絵文字を、主要操作の最終描画に使わない。
   - 意味を持つアイコンはSVGを基本とする。

2. **共通の座標系で描く**
   - SVGは原則 `viewBox="0 0 24 24"`。
   - 幾何学的な中心を同じ座標へ置いたうえで、必要な場合だけ光学補正する。

3. **線の文法をそろえる**
   - 通常の線アイコンは `stroke-width: 2` を基準。
   - FABのように単純で大きく見せたい記号のみ、少し強い線幅を許容する。
   - 端点は原則 `round`。

4. **アイコンの大きさと押せる領域を分ける**
   - 小さな操作アイコン：18px前後。
   - ナビゲーション：20px前後。
   - FAB：24〜25px前後。
   - タップ領域は別に38〜48px以上を確保する。

5. **数値中央のあとに、見た目の重心を確認する**
   - bounding boxが中央でも、線の量や方向で片側が重く見える場合がある。
   - 補正は個別の `translate` を乱立させず、アイコン定義側で行う。

## Implementation rule

- 機能アイコンの正典：`precision-ui.js` の `ICONS`
- サイズ・タップ領域・配置：`precision-upgrade.css`
- 単純な開閉記号：CSS geometryを許可
- テキストとして意味がある記号や絵文字：コンテンツ内では使用可

## Current sizes

- bottom navigation: 20×20
- modal close / clear search: 18×18
- overflow menu: 18×18
- FAB: 25×25
- modal / clear hit target: 38×38以上
- FAB hit target: 58×58

## Applied on 2026-08-13

- FABの「＋」をSVGの正典へ一本化
- 下部ナビのリンク／プロンプト／管理アイコンを共通24×24座標系で維持
- モーダルの「×」と検索クリアをSVGへ統一
- 管理画面の開閉「＋／−」をフォント字形からCSS geometryへ変更
- キャッシュキーを更新

## Review checklist

- [ ] フォントを変えても操作アイコンの位置が変わらない
- [ ] iOS / desktopで記号の重心差が出ない
- [ ] 同じ階層のアイコンで線幅・端点・サイズ感がそろっている
- [ ] アイコン自体を大きくしなくてもタップ領域が十分ある
- [ ] `aria-label` があり、SVGは装飾として `aria-hidden="true"`
- [ ] 200%拡大でもボタン同士が重ならない
- [ ] 個別の微調整値が増えていない
